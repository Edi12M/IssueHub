Phase 3 - Creating services, controllers, dto for notification, attachemments, comments, issuehistory, and fixing the issue service and dto

First of all fixing problems, implementing the new features in the issue
1)Update Issue -> when changinig the status of the issue check if it has subtask
	->if there are subtaskts return list of subissues
	->else
		-change issue data and save in db
		-create issue history and save in db
		-create notification about the issue change: Notification Type = issueupdated. entity type is the issue, entity id is the issue id
		 isRead is false, createdat is the datetime now. save notification in db and send email to all users which are connected with the issue. do this why looking at the issueassingment table. 
		-return success message
2)Return Issue data a specific user
	-add comments list
	-add list of attachemnets

Secondly, creating the new services, dto, controllers etc for the new models
Comments
1)Create comment (there is a create comment button inside the issue page)
	-issueid is the id of the issue
	-authorid is the user is
	-parent id is the id of the comment this comment is a reply to
	-body is the text
	-isEdited = false
	-isDeleted = false
	-createdat = datetime now
	-updated at = datetime now
	->create a notification about comment creation and send email to all users in the issue
2)Return comment data
	-return body
	-return is edited
	-return updated at
3)Update comment
	-check is comment author id is the same as the id of the user requesting to edit
	-can change comment body
	-make the updatedat = datetime now
	-make isedited = true
4)Delete comment
	->if it has replies -> show warining message
	->if user clicked yes button
		-delete comment + replies
		-make isdeleted true for all comments impacted
		-create notification and notify all users in the issue thread
5)Return list of comments for a specific issue
	-comments + replies
	-return only not deleted comments
Attachements
1)Create Attachement
	-validate type and size - return error messasge if it doesn't comply the requirements
	-save attachement in database, uploaded by is user id, issue id, get file name and type and size and put them in fields in the databse
	 store the file in an attachements folder and store the path, uploadedat - datetime now
	-create and send notification for attachement upload to all users in the issue
2)Return attachement data
	-filename, file type
	-uploaded at
3)Return attachement
	-idk how to send the attachement, stored inside the attachement folder, to the front end
4)Delete attachement
	-create and send notification to all users in the issue
Notifications
1)Return notification data
	-type
	-Entity type
	-created at
	-mark isread = true
	-when clicking a button in the front, it can open the issue it is associated with, i dont want the used to see the issue id, how do i do this
2)Return list of notifications for a user
	-type
	-entity type
	created at
IssueHistory
1) return issue history for a specific user
	-ordered by date

