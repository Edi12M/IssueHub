using Backend.Enum;

namespace Backend.Services
{
    public class PermissionService
    {
        public Task RefreshPermissions(int userId)
            => throw new NotImplementedException();

        public Task ApplyNewAccessRules(int userId, UserRole role)
            => throw new NotImplementedException();
    }
}
